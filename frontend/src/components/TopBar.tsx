import React from 'react';
import { Link } from 'react-router-dom';

export default function TopBar() {
  return (
    <div className="bg-black text-white text-[10px] md:text-xs text-center py-2 px-4 tracking-widest uppercase font-medium">
      ENVÍOS GRATIS EN COMPRAS SELECCIONADAS | NUEVA COLECCIÓN DISPONIBLE
    </div>
  );
}
